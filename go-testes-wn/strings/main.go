package main

func main() {
	// s1 := "Hello, world!\nNova linha"
	// s2 := `Hello, world!\nIsso não é uma nova linha`

	// fmt.Println(s1)
	// fmt.Println(s2)

	// s := "Golang"
	// primeiroByte := s[0]

	// fmt.Println(primeiroByte)

	// for i := 0; i < len(s); i++ {
	// 	fmt.Printf("%c ", s[i])
	// }

	// // for index, runeValue := range s {
	// // 	fmt.Printf("Índice: %d, Caractere: %c\n", index, runeValue)
	// // }

	// s := "Golang"
	// bytes := []byte(s)
	// fmt.Println(bytes)

	// s := "Olá, 世界"
	// runas := []rune(s)
	// fmt.Println(runas)

	// var builder strings.Builder
	// for i := 0; i < 10; i++ {
	// 	builder.WriteString("Número: ")
	// 	builder.WriteString(fmt.Sprintf("%d\n", i))
	// }
	// resultado := builder.String()
	// fmt.Println(resultado)
}
