package main /*Pacote padrão*/ /*O pacote main informa ao compilador que será o pacote principal de entrada da aplicação*/

import "fmt" /*Local de importação de pacotes*/ /*fmt é um exemplo de pacote padrão que vem com a linguagem*/

func main() {
	fmt.Print("Hello World")
}
